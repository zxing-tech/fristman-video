# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Static marketing site for **Firstman Videos** (industrial drone cinematography for Oil & Gas / heavy industry). 14 statically-prerendered App Router pages. No backend — forms POST to Web3Forms and redirect to `/thank-you/*` pages. Stack: **Next.js 16.2.6, React 19, Tailwind CSS v4, shadcn (`base-nova`), next-themes**.

## Commands

```bash
npm run dev         # dev server
npm run build       # production build (also runs TS check + prerenders all routes)
npm run start       # serve the production build (default port 3000)
npm run lint        # eslint (eslint-config-next) — flat config, no args
npm run typecheck   # tsc --noEmit
npm run format      # prettier --write on **/*.{ts,tsx}
```

There are **no tests**. After nontrivial changes, verify with `typecheck` + `lint` + `build`, then drive the page in a browser (light AND dark theme) — most bugs here are Video/theme-contrast issues the type system can't catch. Note: `typecheck` reads `.next/types`, so run it *after* `build` when you've added/renamed/moved routes, or it reports stale route errors.

Code style is Prettier-enforced (`.prettierrc`): **no semicolons**, double quotes, 2-space indent, 80 columns, ES5 trailing commas. `prettier-plugin-tailwindcss` sorts class strings, including inside `cn()` and `cva()`.

## Next.js 16 — read the bundled docs first

This is **not** the Next.js in your training data. Per `AGENTS.md`, read the relevant guide in `node_modules/next/dist/docs/` before writing Next-specific code. Known differences that bite:
- `params` and `searchParams` are **Promises** — `await` them.
- `next/image` `priority` is removed → use `preload`.
- Pages are Server Components by default; only interactive files (`"use client"`) are client (navbar, theme toggle, modals, forms, case-study filter, video lightbox, cookie banner).

## Theme system (the core architecture)

Light/dark is driven entirely by CSS variables in `app/globals.css` that **flip** between `:root` (light) and `.dark` (dark). `next-themes` (`components/theme-provider.tsx`) toggles the class; `components/site/theme-toggle.tsx` is the button. The layout mounts it as `attribute="class" defaultTheme="light" enableSystem={false}` (**light since 2026-08-07**, dark before that) — the site never follows the OS preference, so testing means clicking the toggle, not changing OS settings. `defaultTheme` only decides a *first* visit: next-themes persists the choice, so a browser that toggled to dark before the switch still opens in dark until its site data is cleared.

Brand tokens (defined in both `:root` and `.dark`, wired into Tailwind via `@theme inline`):
- `background` / `surface` **invert** — `surface` is the foreground/text color (white in dark, near-black in light). `background` is the page (black in dark, white in light).
- `dark-ui`, `graphite`, `graphite-light`, `industrial-grey` also flip; `primary` (`#d12027` red) is constant.
- Custom utilities in `globals.css`: `.glass-panel`, `.glass-modal`, `.form-input-dark`, `.hero-overlay` (bottom scrim that fades a hero photo into `--background`), `.scanning-line`, `.scanner-line`, `.no-scrollbar`, `.text-gradient` (fixed light gradient — only used on dark hero headings), `.marquee` / `.marquee-track` / `.animate-marquee-left|right`.
- Two global rules that surprise people: `html { overflow-x: clip }` is a deliberate safety net against decorative overhangs (`clip`, not `hidden`, so `sticky`/`fixed` keep working), and a `body *` rule transitions `background-color/border-color/color/box-shadow/fill` over 0.3s — every element animates on a color change, not just the theme flip.

### Rules when editing markup (critical — this is where regressions happen)
- **Use semantic tokens, not hardcoded neutrals.** `bg-background` not `bg-black`, `text-surface` not `text-white`, `border-surface/10` not `border-white/10`, `text-industrial-grey` for secondary text. These flip with the theme; hardcoded `white`/`black` do not.
- **Exception — dark photo areas.** Heroes/cards backed by a photo stay dark in *both* themes. Their wrapper carries a dark base (`bg-black`) and the image sits behind text. Over these: keep literal `text-white` on headings, use `text-white/70` for body copy (NOT `text-industrial-grey`, which goes dark in light mode and becomes unreadable), and keep `from-black` gradient scrims. `text-white` on `bg-primary` buttons also stays literal.
- **Faint photos wash out in light mode.** Photo images use the pattern `opacity-90 dark:opacity-40` (+ `mix-blend-normal dark:mix-blend-luminosity` where applicable) so the photo is visible in light mode but stays subtle in dark. A pure `bg-black` + low-opacity image + `z-[-1]` will render invisible — image layers belong at `z-0` with content at `z-10`.

## Layout & shared chrome

`app/layout.tsx` mounts `<SiteNavbar>` + children + `<SiteFooter>` inside `<ThemeProvider>`, loads Archivo and IBM Plex Mono (`next/font`) plus Material Symbols (a `<link>` in `<head>` — kept as a stylesheet, not `next/font`, so `font-variation-settings` FILL animates). Archivo is requested with `axes: ["wdth"]`; drop that and `font-stretch-*` on the display `h1`s silently stops doing anything. IBM Plex Mono is wired to `--font-mono` and belongs only on HUD readouts (reference codes, coordinates, statuses). Global metadata uses a title template `%s | Firstman Videos`.

Reusable pieces live in `components/site/`: `SiteNavbar` (**`fixed`**, floats over the hero — so full-bleed hero pages let the image reach the top, and content pages add `pt-28`/`pt-32` so breadcrumbs clear it), `SiteFooter`,

`SiteNavbar`'s brand is the owner's wordmark image as of 2026-08-27, not the Archivo text lock-up it replaced — see the brand-logo note under *Data, images, forms* for why it is a CSS background rather than an `<img>`.

`SiteNavbar`'s **Services item is a dropdown, not a link** — there is no `/services` hub. It maps over `lib/data/services.ts`, so adding a service to that array is all it takes to appear in the nav. The desktop panel is always rendered and hidden with `invisible`/`opacity-0` rather than conditionally mounted: that keeps the six links in the static HTML for crawlers, and `visibility: hidden` still removes them from the tab order while closed. Mobile uses a separate accordion inside the burger sheet. Note the eslint rule `react-hooks/set-state-in-effect` is on — do not close the menus from a `useEffect` on `pathname`; the link `onClick` handlers already do it. `ThemeToggle`, `CtaButton`, `ServiceCard`, `SectionLabel`, `MaterialIcon`, `CaseStudiesPortfolio`, and the `RequestAccess*` trio. `MaterialIcon` wraps a `material-symbols-outlined` span. Google's icon stylesheet is an **unlayered** `<link>` in `<head>`, and unlayered declarations beat everything in Tailwind's `@layer utilities` regardless of specificity or source order. Two consequences: toggle icon *visibility* on a plain wrapper span, never on the icon itself (it forces `display`); and a bare `text-3xl`/`text-4xl` on an icon **does nothing** — the sheet pins `font-size: 24px`. Use the important modifier (`text-3xl!`) to size an icon, and verify with `getComputedStyle`, not by reading the class. As of 2026-08-04 most icons outside `app/page.tsx`'s "Why Firstman Videos" section still carry dead size classes and all render at 24px.

Two files are currently **unused** — don't read them as the house pattern: `components/site/case-study-card.tsx` and the lone shadcn `components/ui/button.tsx`. Buttons in this codebase are `CtaButton` or hand-written pill classes on `<Link>`/`<button>`.

## Data, images, forms

- `lib/data/{services,sectors}.ts` — typed arrays. **`services` has four readers and no hardcoded copies left**: `app/sitemap.ts`, the navbar Services dropdown, the homepage "Our Core Services" grid, and the Primary Interest `<select>` on the contact form (wired up 2026-08-27; it was the last hand-maintained duplicate and had drifted). Renaming a service in this file is enough for all four. (`lib/data/our-work.ts` went with the case-study detail routes on 2026-08-06 and **came back on 2026-08-07**: it holds every film on `/our-work` — public and private alike — read by the client-side grid, the server component that emits their `VideoObject` JSON-LD, and, since 2026-08-27, the per-service `ServiceFilms` strip. It is not read by `sitemap.ts` — nothing under `/our-work` has a route.) **`sectors.ts` is the odd one out**: its four entries (Oil & Gas, Energy & Utilities, Marine & Offshore, Fabrication & Construction) have **no routes** — they survive the removed `/industries` pages as content only, and `app/page.tsx` is now their only reader. It is deliberately absent from `sitemap.ts`; don't add `href`s to it without creating pages first.
- `lib/data/image-manifest.json` maps the original design-export image URLs to the downloaded local paths. It is a reference artifact from the conversion — nothing imports it.
- **`/our-work` is the one hub whose body is a client component.** `lib/data/our-work.ts` is now the single source for every film — `PortfolioFilm` is a discriminated union on `access: "public" | "private"`, and `components/site/our-work-portfolio.tsx` renders each entry as one of three things: the page's one **featured** slot (`access: "public"` + `featured: true`, at most one, enforced by a runtime check at the bottom of the data file — a large hero panel with a Play button, above the grid), a normal **public** grid card (`PortfolioCard`, opens the film in the lightbox), or a **private** grid card (`GatedFilmCard`, a locked plate + Request Access by default). A chip only matches a film whose `category` is that exact string, so `FILM_CATEGORIES` and `PortfolioFilm["category"]` are the same strings by construction. Otherwise a filter drops to the "No case studies in this category yet" empty state. **There are no case-study detail pages** and no plans to reintroduce one — a card is not a link.
  - **The roster went from 9 films to 28 on 2026-08-27** (16 public, 12 gated), from the owner's `YouTube Source Files.rtf` in the 2026 asset delivery. Every id was verified through oEmbed, every thumbnail is a local copy of the film's own, and the five `FILM_CATEGORIES` are now five of the six service titles verbatim — which is what lets each service page render its own strip. **`(LOCKED)` in the owner's source list means gated**, confirmed by the owner: three films that were live and public (Jerun, Sapura KG-DWN-98/2, MMHE/Hess/Petronas) are now `access: "private"`. The one invented entry, `confidential-oag`, was removed — with 28 real films on the page there is no reason to keep a fabricated 29th. Two films in the source list were deliberately left out and are noted at the top of the data file: "4-day Forum Highlights" (no client named) and "OFFSHORE WEST DESARU" (403 from YouTube, no thumbnail).
  - **`components/site/service-films.tsx` is the same data on a service page**, as a 16:9 tile reel rather than the hub's card. Public tiles open the shared lightbox; gated tiles open Request Access. It takes `category` (the service's own title) and an optional `limit`. Deliberately not the `/our-work` card: that card carries a paragraph of summary, which is right on the hub and wrong between two blocks of prose.
  - **Private films get a preview, not just a lock, once one exists.** `PrivateFilm.previewYoutubeId` is optional and unset on every entry as of 2026-08-11; when unset, `GatedFilmCard` shows the original dimmed-still-plus-lock-icon treatment. The moment a film gets a real `previewYoutubeId`, the same card switches to a Play button that opens a short public preview in the lightbox, with the Request Access CTA still present below it (a preview is not full access). This is a one-line data change, not a component change — don't add a second card component for it.
  - **`FIRSTMAN_LOGO_SRC` in `our-work-portfolio.tsx` was filled on 2026-08-27** with `/images/brand/logo-dark.png` — the light-ink variant unconditionally, because the card it sits in stays dark in both themes. It still renders nothing today: it only appears over a private film's *preview* clip, and no entry has a `previewYoutubeId` yet.
  - A public card's stretched `<button>` opens `components/site/video-lightbox.tsx`, which mounts a `youtube-nocookie.com` iframe for whatever `{ youtubeId, label }` pair is currently playing (a public film's `youtubeId`, or a private film's `previewYoutubeId`) and holds nothing else — no title, no specs, no copy. The iframe exists only while something is playing, so a visitor who never presses play makes no request to Google; keep it that way rather than pre-rendering embeds. `app/our-work/page.tsx`'s `VideoObject` JSON-LD is filtered to `access === "public"` only — a private film has no publicly embeddable video to describe, so schema claiming otherwise would be false; that filter is the thing to revisit once real preview clips start landing.
  - Adding a public film means one entry in `lib/data/our-work.ts` plus its thumbnail under `public/images/our-work/`. Making a film private means flipping `access` and dropping `youtubeId`/`uploadDate` — TypeScript enforces that a private film can't carry a public film's fields. Moving the `featured: true` flag is how "swap this film with the featured one" gets done; the runtime check throws immediately if two entries ever carry it at once.
- Imagery is all local under `public/images/` (`stitch/` = page photos, `clients/` = clientele/agency logos, `our-work/` = film thumbnails, `brand/`, `og/`). Most photos are `<div style={{ backgroundImage: "url(...)" }}>` + `bg-cover`, **not** `next/image` — only four pages import it, and the homepage logo marquee uses raw `<img>` behind an eslint-disable. Match the surrounding pattern instead of converting files ad hoc. Client logos are transparent grayscale PNGs shown with `dark:invert` so they read in both themes, and as of 2026-08-27 they run **uncaptioned** on the owner's instruction (`01_Amendments.png`) — the client name moved back onto each `<img>`'s `alt`, which is the only place a screen reader or image search can still read it.
- **`public/images/brand/logo.png` and `logo-dark.png` are the real wordmark**, generated 2026-08-27 from the owner's `FMP Logo transparent2.png` (cropped to the ink, alpha ramp normalised off its 224 ceiling, and the mark's own `#cc0000` dot mapped to Signal Red `#d12027` so it does not clash with the CTA beside it in the navbar pill). The navbar and footer render them as a **CSS background on an empty span**, not `<img>`: the two variants are class-switched by `dark:`, and a background on a rule that does not match is never fetched, where a hidden `<img>` still is. Each carries an `sr-only` name beside it.
- The one lead form, `app/contact/consultation-form.tsx`, is `"use client"`, validates client-side, POSTs to Web3Forms, then `router.push()`es to a thank-you route. Its optional **Sector** `<select>` is where the industry taxonomy went when `/industries` was removed — keep its options in sync with `lib/data/sectors.ts`, and the Region / Country options in sync with the `regionalCoverage` array in `app/page.tsx`. The access key comes from `NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY` (inlined at build — set it before `npm run build`; see `.env.example`).
- The gated-video modal is **split for bundle size**: `request-access-modal.tsx` is the thin trigger + `open` gate, and `request-access-dialog.tsx` holds the heavy form, pulled in via `next/dynamic(..., { ssr: false })` only after a click. Import `RequestAccessModal`/`RequestAccessTrigger`; add form fields to the *dialog*. It posts to Web3Forms in place (no redirect) and renders its own success state.
- `app/privacy-policy/cookie-banner.tsx` is presentational only — its buttons just hide it via local state. Nothing is persisted and no scripts are gated behind consent.

## SEO & metadata (`lib/seo.ts`)

Every page's metadata goes through the `pageMeta({ title, description, path, ogImage?, ogType?, noindex? })` helper — **do not** hand-write a `Metadata` object per page. Next.js overwrites `openGraph` wholesale in child segments, so `pageMeta()` builds the full canonical + Open Graph + Twitter block consistently. `metadataBase` (the canonical domain `SITE.url`) lives in `app/layout.tsx`, which also emits site-wide **Organization + WebSite** JSON-LD and the default OG image.

- **New page checklist:** `export const metadata = pageMeta({ description /* 140-160 chars */, path: "/its/canonical/path", title? })`; add page-type JSON-LD via `<JsonLd data={graph(breadcrumbSchema([...]), serviceSchema(...) | creativeWorkSchema(...))} />` (import from `@/lib/seo` + `@/components/seo/json-ld`) as the first child of the returned element; ensure exactly **one `<h1>`** (use an `sr-only` h1 if the hero has no textual heading); add the route to `lib/data/*` (for the sitemap) *and* to the relevant hardcoded listing grid; thank-you/utility pages pass `noindex: true` and skip JSON-LD.
- `app/robots.ts`, `app/sitemap.ts`, `app/manifest.ts` and `app/favicon.ico` are file-convention routes. Case-study pages set `ogType: "article"` + a page-specific `ogImage`.
- Site is single-locale (English) by design — no hreflang. Descriptions must stay unique and avoid AI-writing tells (em dashes, "comprehensive/seamless/leverage").

## Route map & content notes

Routes mirror the folder tree under `app/`: `/`, `/about` + `/about/team`, 6 service detail pages under `/services/*`, `/our-work`, `/contact`, `/privacy-policy`, `/thank-you/{consultation,case-study-access}`.

**The six services were re-cut on 2026-08-27** from the owner's marked-up navbar (`xxx_Website 2026 Assets/02_Amendments.png`). They are, in the owner's order: **Corporate Videos**, **O&G Topside Loadout** (`/services/og-topside-loadout`), **O&G Offshore Installation** (`/services/og-offshore-installation`), **AI-Generated Videos** (`/services/ai-generated-video`, slug kept), **Long-Term Timelapse** (`/services/long-term-timelapse`), **Safety and Induction Videos**. Facility Overview Films and Industrial Photography were retired; `progression-timelapse` was renamed. All three old URLs 308 in `next.config.ts` (`facility-overview-films` → `og-topside-loadout`, `industrial-photography` → `/#services`, `progression-timelapse` → `long-term-timelapse`). Do not reintroduce the two retired names as service labels.

**Two former hubs no longer exist, and both now 308-redirect to a homepage section** — `/services` → `/#services` and `/industries` (plus `/industries/:slug`) → `/#industries`. Both `id`s live on `<section>`s in `app/page.tsx` with `scroll-mt-28` to clear the fixed navbar. `app/services/` holds only the six detail folders, so service detail pages breadcrumb straight to Home; nothing should link to a bare `/services` or any `/industries` URL again.

**The six case-study detail pages were removed on 2026-08-06** and `/our-work/:slug` 308-redirects to `/our-work`; a card there opens a video lightbox instead of navigating. That deleted `app/our-work/{petrofac-kemaman,mahb-airport-services,ecobalance,servishero-campaign,syndel-asia,ben-line-agencies}/`, `lib/data/our-work.ts`, and the two components only those pages used (`case-specs.tsx`, `related-services.tsx`). It also took the site's last `creativeWorkSchema` call sites — the helper is still exported from `lib/seo.ts` but nothing emits `CreativeWork` JSON-LD any more, which was the point: those six pages named real companies as clients in machine-readable form and none of the projects were real (PRODUCT.md `## Evidence on Hand`). `/case-studies/:slug` now redirects straight to `/our-work` rather than through `/our-work/:slug`, so an old URL resolves in one hop.

The `/industries` hub and its four sector pages (oil-gas, energy-utilities, marine-offshore, fabrication-construction) were removed and their content redistributed rather than deleted: the sector taxonomy went to the homepage `#industries` grid (via `lib/data/sectors.ts`) — its second reader, the "Designed For" list, went with `/services/facility-overview-films` on 2026-08-27, so `app/page.tsx` is now the only file rendering `sectors`; regional coverage went to the homepage; the pre-mobilisation and confidentiality material (permit-to-work, HSE alignment, weather windows, NDAs, encrypted transfer, gated portals) became two new **About** sections; the fabrication load-out phase became Phase 04 on `/services/long-term-timelapse`; and the sector taxonomy also survives as the Sector `<select>` on the contact form. The marine brief form went away with its page — `/contact` is now the only lead form. An earlier "Industrial Cinematography" service was renamed and an "FPV Fly-Throughs" service was dropped, so avoid reintroducing those names as service labels (the brand tagline "Industrial Cinematography" is fine).

**Service renames now land in three places, not five.** The hardcoded copies are gone: the homepage grid maps `services` directly and the contact form's Primary Interest `<select>` maps it too as of 2026-08-27 (it had drifted twice and was offering two services that no longer existed). What is left to change on a rename is `lib/data/services.ts` (slug + href + title — this drives `sitemap.ts`, the navbar dropdown, the homepage grid and the contact select), the detail page's own folder name / `pageMeta` path / breadcrumb / `serviceSchema`, and a `permanent: true` entry in `next.config.ts` `redirects()`. Note `permanent: true` emits **308**, not 301.

Two invariants a rename must not break, both enforced by hand rather than by the compiler:
- **`services.ts` `title` must equal the page's `pageMeta` title**, and all six now do.
- **`FILM_CATEGORIES` in `lib/data/our-work.ts` must match five of those titles character for character** — `ServiceFilms` looks a page's films up by passing its own service title through, so a mismatch empties the strip silently rather than failing the build. AI-Generated Videos is deliberately absent from that list (no samples yet, so no strip on that page).

Grep the old slug *and* the old display label before declaring a rename done — stale labels hide in section headings.

`README.md` is still the stock shadcn starter template and describes nothing about this site — ignore it. Background/spec: `docs/superpowers/specs/2026-07-06-stitch-to-next-conversion-design.md` documents the original conversion and full route map.
